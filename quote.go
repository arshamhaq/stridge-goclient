package stridge

import "context"

// Quote will request a cross-chain quote.
func (c *Client) Quote(ctx context.Context, req QuoteRequest) (*Quote, error) {
	// TODO: Implement the Quote endpoint using c.do.
	return nil, ErrNotImplemented
}
